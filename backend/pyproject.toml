[tool.ruff]
line-length = 200
target-version = "py311"
exclude = ["tests/", "__pycache__/", "venv/"]

[tool.ruff.lint]
select = ["E", "F", "W"]
# No ignores — codigo 100% limpio bajo PEP8 (con line-length razonable
# para strings largos en español y prompts de IA)
